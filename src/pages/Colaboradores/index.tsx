/**
 * Colaboradores/index.tsx — Re-exports ColaboradoresPage as the /colaboradores route.
 * Standalone (sem MainLayout): gerencia seu próprio header, footer e dark mode.
 */
export { default } from "../../ColaboradoresPage";
